# CLAUDE.md

스웨덴어–한국어 회화 학습 사이트. 서버도 빌드 단계도 없는 정적 사이트이고,
내용 전부가 `data/entries.json` 한 파일에서 나옵니다.

**이 저장소에서 가장 자주 하는 일은 사용자가 준 한국어 문장을 항목으로 만들어
`data/entries.json` 에 덧붙이는 것입니다.** 아래 규칙을 그대로 따르세요.

---

## 문장 추가 — 기본 작업

사용자가 한국어 문장이나 낱말을 주면 (예: "이거 추가해줘: 이 자리 비었나요",
또는 그냥 문장만 던져도) 다음 순서로 처리합니다.

1. `data/entries.json` 을 읽습니다.
2. 아래 형식에 맞는 항목을 만들어 **배열 끝에 덧붙입니다.** 기존 항목은 건드리지 않습니다.
3. `python3 tools/validate.py` 를 돌립니다. 반드시 통과해야 합니다.
4. 커밋하고 `main` 에 푸시합니다. 푸시하면 GitHub Actions 가 자동으로 배포합니다.
5. 사용자에게 **바뀐 개수**를 알려 줍니다 (문장 N→M, 낱말 N→M, 분류 N→M).
   사이트 위쪽에 이 숫자가 늘 떠 있고, 사용자는 이걸 보고 더 채울지 정합니다.

여러 문장을 한 번에 주면 전부 항목으로 만들고 한 커밋으로 묶습니다.

### 항목 형식

`data/SCHEMA.md` 에 전체 명세가 있습니다. 요지:

```jsonc
{
  "id": "kan-jag-fa-notan",   // 스웨덴어 문장을 로마자 소문자+하이픈으로. 파일 안에서 유일해야 함
  "added": "2026-09-17",      // 오늘 날짜
  "kind": "phrase",           // phrase(문장) | word(낱말 하나)
  "category": "카페·식당",      // 아래 목록에서 고르기
  "source": "",               // 출처. 직접 쓴 것은 "". 아래 "출처" 참고.
  "ko": "계산서 주시겠어요?",    // 사용자가 준 한국어. 다듬되 뜻은 바꾸지 않기
  "romanization": "",         // 보통 빈 문자열
  "sv": "Kan jag få notan, tack?",  // 스웨덴 사람이 실제로 쓸 법한 자연스러운 번역
  "literal_sv": "",           // 직역이 구조 이해에 도움 될 때만. 아니면 ""
  "meaning": "식당에서 계산할 때 쓰는 표현.",  // 언제 어떻게 쓰는지
  "ipa": "kan jɑː foː ˈnuːtan tak",   // 아래 "발음" 참고 — 손으로 쓰지 말 것
  "ko_pron": "칸 야- 포- 누-탄 탁",
  "words": [ /* 아래 규칙 */ ],
  "notes": [ /* 문법 노트. 없으면 [] */ ],
  "examples": [{ "sv": "...", "ko": "..." }],
  "equivalent": { "sv": "", "literal": "", "note": "" },
  "tags": ["필수", "패턴"]
}
```

### `words` — 가장 중요한 부분

**`words` 는 `sv` 에 쓰인 스웨덴어 낱말입니다. 한국어 낱말이 아닙니다.**
사이트의 단어장 탭이 이걸 `base` 기준으로 모아 보여 주기 때문입니다.

- `sv` 에 나온 순서대로, 빠짐없이 적습니다.
- `surface` 는 문장에 나타난 그대로 (대문자도 그대로).
- `base` 는 사전형. 같으면 같은 값을 넣습니다.
- 사람 이름 같은 자리표시자는 **넣지 않습니다.** 단어장을 더럽힙니다.
- 관용구처럼 굳어진 덩어리는 쪼개지 말고 한 항목으로 두고 `note` 에 밝힙니다.

`pos` 표기는 아래를 따릅니다. 일관성이 단어장의 값어치를 만듭니다.

| 품사 | 쓰는 법 | 예 |
|---|---|---|
| 명사 | 성과 복수형을 함께 | `명사 (en-단어, 복수 -ar)`, `명사 (ett-단어, 복수 ord)` |
| 명사 (정관사형) | 정관사가 붙은 형태면 | `명사 (en-단어, 정관사형)` + `note: "nota → notan"` |
| 동사 | 형태를 밝히고 `note` 에 원형·과거형 | `동사 (현재형)` + `note: "원형 skydda, 과거형 skyddade"` |
| 동사 (원형) | 조동사 뒤 등 | `동사 (원형)` |
| 조동사 | | `조동사 (현재형)` |
| 형용사 | 중성형·복수형이면 밝히기 | `형용사 (중성형)`, `형용사 (비교급)` |
| 대명사 | 목적격이면 밝히기 | `대명사`, `대명사 (목적격)` |
| 그 외 | | `전치사`, `부사`, `접속사`, `부정관사`, `감탄사`, `의문부사` |

### 발음 — 손으로 쓰지 말고 생성하세요

`ipa` · `ko_pron` · 낱말의 `ipa` 는 **직접 타이핑하지 않습니다.**
`tools/ipa.py` 의 낱말 사전에서 조립합니다.

새 문장에 사전에 없는 낱말이 있으면:

1. `tools/ipa.py` 의 `WORDS` 에 `"낱말": ("IPA", "한글근사")` 를 더합니다.
   (고유명사는 `EXTRA` 에 넣습니다 — 단어장에는 안 들어가지만 문장 발음에는 필요합니다.)
2. `python3 tools/regen-pron.py` 로 전체를 다시 생성합니다.
3. `python3 tools/validate.py` 로 확인합니다. 사전에 없으면 `?` 가 남아 실패합니다.

기준은 중앙 표준 스웨덴어입니다. 한글 근사에서 장모음은 `-` 로 표시합니다.
`y`·`u`(ʉ)·`ö`·`sj` 는 한글로 정확히 못 옮기니 가장 가까운 글자를 쓰고,
헷갈릴 만하면 `notes` 에 한 줄 적어 줍니다.

### `notes` — 문법을 문장 안에서 가르치기

문법책을 따로 두지 않고 문장마다 걸리게 하는 것이 이 사이트의 방식입니다.
그 문장이 실제로 보여 주는 것만 적습니다. 억지로 끼워 넣지 않습니다.

자주 나오는 주제: V2 어순, 후치 정관사, en/ett 성, 조동사 뒤 원형,
`inte` 위치(주절/종속절), `-s` 동사(수동·상호·디포넌트), 완료형, 비교급,
재귀동사, 구동사, 합성어, 목적격 대명사.

### `category`

기존 이름을 쓰면 칩이 늘지 않아 보기 좋습니다. 맞는 게 없을 때만 새로 만듭니다.

```
인사 · 소개 · 언어·소통 · 예의 · 카페·식당 · 쇼핑 · 길·교통
약속·시간 · 감정·반응 · 건강·곤란 · 스몰토크 · 숙소·생활 · 자연·풍경
기타
```

`기타` 는 일상에서 쓸 일이 없는 것들을 모아 둔 자리입니다.
**오늘의 표현 로테이션에서 빠집니다** (`js/entries.js` 의 `NOT_DAILY`).
모아보기와 단어장에는 그대로 들어갑니다.

### `source` — 출처

어디서 가져온 내용인지 남깁니다. **분류와는 별개입니다.**

- 직접 쓴 문장은 `""`.
- 어딘가에서 옮겨 온 것은 그 출처 이름 (지금은 `"dialogue"` 하나).

분류는 쓸모에 따라 여기저기로 흩어져도, `source` 는 그대로 남습니다.
그래서 나중에 한 출처만 통째로 걷어낼 수 있습니다:

```bash
python3 tools/remove-source.py dialogue --dry-run   # 몇 개 지워질지 보기
python3 tools/remove-source.py dialogue             # 실제로 지우기
```

새 출처를 쓰면 `tools/validate.py` 의 `KNOWN_SOURCES` 에도 더해 주세요.

새 분류를 만들면 `tools/validate.py` 의 `KNOWN_CATEGORIES` 에도 더해 주세요.
(경고만 뜨고 실패하지는 않지만, 오타를 걸러 주는 장치입니다.)

### 스웨덴어 품질

- 스웨덴 사람이 실제로 쓸 법한 문장을 씁니다. 한국어 어순을 그대로 옮기지 않습니다.
- **확신이 서지 않으면 `notes` 에 적어 둡니다.** 예: "북유럽의 높은 산은 berg 보다
  fjäll 로 부르는 일이 많습니다." 조용히 넘기는 것보다 낫습니다.
- 더 자연스러운 대안이 있으면 `equivalent` 에 넣습니다.
- `examples` 는 그 표현이 실제로 쓰이는 짧은 대화 한두 개로 만듭니다.

---

## 작업 뒤 확인

```bash
python3 tools/validate.py     # 항목을 고쳤으면 반드시
python3 -m http.server 8000   # 화면 확인 (file:// 로는 안 열립니다)
```

푸시 전에 검사기를 돌립니다. CI 도 같은 스크립트를 돌리므로, 여기서 통과하면
배포에서도 통과합니다.

---

## 구조

```
data/entries.json   ← 사이트의 모든 내용. 대부분의 작업이 여기서 끝납니다
data/SCHEMA.md      형식 명세
tools/validate.py   검사기 (로컬·CI 공용)
tools/ipa.py        낱말별 IPA·한글 근사 사전
tools/regen-pron.py 발음 재생성
tools/remove-source.py  특정 출처 항목 일괄 제거

index.html          화면 구조
css/style.css       스타일 (라이트/다크)
js/app.js           탭·검색·설정 배선
js/entries.js       데이터 읽기, 날짜별 선택, 검색, 단어장·분류 집계
js/render.js        데이터 → DOM
js/store.js         localStorage 래퍼
js/speech.js        발음 듣기 (브라우저 내장 음성 합성, 외부 API 없음)

── 번역기 탭에서만 쓰입니다 (선택 기능) ──
js/api.js js/schemas.js js/models.js js/usage.js
vendor/anthropic-sdk.js   브라우저용으로 번들한 공식 Anthropic SDK
tools/build-vendor.sh     위 번들 재생성
```

**SDK 는 동적 import 로 불러옵니다** (`js/api.js` 의 `loadSdk`). 194KB 나 되는데
번역기 탭은 선택 기능이라 대부분의 방문은 쓰지 않습니다. 정적으로 import 하면
안 쓰는 사람에게까지 받게 하니, 이 구조를 되돌리지 마세요.

사이트의 기본 기능은 **API 키 없이 동작합니다.** 번역기 탭만 사용자 본인의
Anthropic API 키를 씁니다. 이 구분을 깨뜨리지 마세요 — 키 없이도 오늘의 표현,
모아보기, 단어장, 발음 듣기가 전부 동작해야 합니다.

발음 듣기는 브라우저 내장 음성 합성(Web Speech API)을 씁니다. 외부 TTS API 를
붙이지 마세요 — 키가 필요해져서 위 원칙이 깨집니다. 기기에 스웨덴어 음성이
없으면 버튼을 숨기고 안내를 띄웁니다. 엉뚱한 언어 음성으로 스웨덴어를 읽어
주면 발음을 배우는 데 해롭기 때문입니다.

`js/speech.js` 를 고칠 때 지킬 것:

- **voice 객체를 캐시하지 마세요.** 읽기 직전에 `getVoices()` 에서 새로 찾습니다.
  브라우저가 목록을 다시 채우면 예전 객체가 무효가 되고, 그때 조용히 시스템
  기본 음성(한국 사용자면 한국어)으로 넘어가 버립니다.
- `utterance.voice` 와 `utterance.lang` 을 **둘 다** 지정합니다. voice 를 무시하고
  lang 으로만 고르는 엔진이 있습니다.
- `voiceschanged` 를 안 쏘는 브라우저가 있어 처음 몇 초는 폴링도 함께 합니다.
- 설정에 음성 선택기가 있습니다. 어떤 음성이 실제로 쓰이는지 사용자가 눈으로
  확인하고 바꿀 수 있어야 합니다 — 이런 문제는 안 보이면 진단이 불가능합니다.

---

## 하지 말 것

- **API 키를 코드나 커밋에 넣지 않습니다.** 키는 사용자 브라우저의
  `localStorage` 에만 있습니다.
- **모델이 만든 문자열을 `innerHTML` 로 넣지 않습니다.** `textContent` 만 씁니다.
  (`js/render.js` 가 이미 그렇게 되어 있습니다.)
- `data/entries.json` 의 기존 항목을 요청 없이 고치거나 지우지 않습니다. 덧붙이기만 합니다.
- 요청받지 않은 PR 을 만들지 않습니다.
- **절대 경로를 쓰지 않습니다.** `css/style.css` 처럼 상대 경로로만 참조합니다.
  `/css/style.css` 로 쓰면 개인 도메인의 하위 경로(`example.com/translator/`)에
  배포했을 때 404 가 납니다. 지금은 전부 상대 경로이고, 하위 경로에 올려
  브라우저로 확인해 두었습니다.

---

## 배포

`main` 에 푸시하면 `.github/workflows/pages.yml` 이 GitHub Pages 로 배포합니다.
배포된 주소는 저장소 Settings → Pages 에서 확인하세요.

Pages 를 처음 켤 때만 저장소 소유자가 Settings → Pages → Source 를
"GitHub Actions" 로 바꿔야 합니다. 워크플로 토큰에는 그 권한이 없습니다.
